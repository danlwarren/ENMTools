rm(list=ls())
setwd("~/Google Drive/R Projects/ENMTools")
source("ENMToolsTest.R")
package.skeleton(name="ENMTools")

#Next steps:
#Edit doc files
#Navigate to directory in Terminal, type "R CMD BUILD ENMTools"
#This produces a bundled tar.gz