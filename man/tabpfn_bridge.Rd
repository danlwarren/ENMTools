% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enmtools.tidy.R
\name{tabpfn_bridge}
\alias{tabpfn_bridge}
\title{Wrapper function for TabPFN classifier}
\usage{
tabpfn_bridge(
  x,
  y,
  backend = "local",
  model_path = "auto",
  device = "auto",
  n_estimators = 8L,
  softmax_temperature = 0.9,
  balance_probabilities = FALSE,
  average_before_softmax = FALSE,
  ensemble_subsamples = NULL,
  ...
)
}
\arguments{
\item{x}{Matrix or data.frame of environmental variables at points}

\item{y}{Single column matrix or data.frame containing a two-level factor}

\item{backend}{Character. Either "local" or "api".}

\item{model_path}{Character. Model path: "auto" (default pretrained), "real" (real-data pretrained),
a finetuned model name (e.g. "sdm-finetuned-nonspatial"), or a file path to a .pt checkpoint.}

\item{device}{Character. Device for computation: "auto", "cuda", or "cpu".}

\item{n_estimators}{Integer. Number of ensemble estimators.}

\item{softmax_temperature}{Numeric. Softmax temperature for predictions.}

\item{balance_probabilities}{Logical. Whether to balance class probabilities.}

\item{average_before_softmax}{Logical. Whether to average before softmax.}

\item{ensemble_subsamples}{Integer or NULL. Number of subsamples for API manual ensembling.}

\item{...}{Additional arguments (ignored).}
}
\value{
A list with class "tabpfn_fit" containing the fitted model and metadata.
}
\description{
Wraps the TabPFN Python package for use in a \code{parsnip} model specification.
Supports three backends: local pretrained, local finetuned, and cloud API.
Mostly for internal use. Exported so it works properly with parallel computation in \code{tidymodels}.
}
